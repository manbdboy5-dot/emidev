# emidev - Multi-Protocol V2Ray/Xray Installer

এক ক্লিকেই আপনার সার্ভারে V2Ray/Xray এর মাধ্যমে মাল্টি-প্রোটোকল সার্ভার সেটআপ করুন।

## 🚀 দ্রুত ইনস্টলেশন (সবচেয়ে সহজ উপায়)

আপনার সার্ভারে SSH করুন এবং নিচের কমান্ডটি রান করুন:

```bash
sudo apt update && sudo apt install curl -y && bash <(curl -s -L https://raw.githubusercontent.com/manbdboy5-dot/emidev/main/install.sh)
